# 运行时依赖（开发依赖见 pyproject.toml [project.optional-dependencies] dev）
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
sqlalchemy>=2.0
aiosqlite>=0.20
pydantic>=2.0
pydantic-settings>=2.0
jinja2>=3.1.0
aiofiles>=24.0
python-multipart>=0.0.20
litellm>=1.0
aiohttp>=3.9.0
pyyaml>=6.0
pyjwt>=2.8.0
reportlab>=4.0
docker>=7.1.0
psutil>=7.0

# ---- 多语言 AST 解析（可选，未安装时自动降级为正则）----
tree-sitter>=0.23.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0
tree-sitter-java>=0.23.0
tree-sitter-go>=0.23.0
tree-sitter-cpp>=0.23.0

# ---- HTML 精确解析（爬虫，可选，未安装时降级为正则）----
selectolax>=0.3.21

# ---- PostgreSQL 后端（可选，未安装时使用 SQLite）----
asyncpg>=0.30.0

# ---- LangGraph + LangChain（多 Agent StateGraph 编排，可选）----
langgraph>=0.2.0
langchain-core>=0.3.0

# ---- 向量数据库 + Embedding（RAG 语义检索，可选，未安装时降级为 TF-IDF）----
chromadb>=0.5.0
sentence-transformers>=3.0.0
